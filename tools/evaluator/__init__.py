"""The submission contract, as much of it as a student needs.

Only `metadata` and `submissions` ship in the student distribution:
what a submission folder may hold, and the metadata reader that goes
with it. Both are copied unchanged from the platform, so a submission
this accepts is one the marker accepts.
"""
